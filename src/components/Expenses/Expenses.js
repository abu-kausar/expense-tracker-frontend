import React from 'react';
import styled from 'styled-components';

const Expenses = () => {
    return (
        <ExpensesStyeled>
            expense
        </ExpensesStyeled>
    );
};

const ExpensesStyeled = styled.div`
`

export default Expenses;